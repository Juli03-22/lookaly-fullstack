# =============================================================================
# .env.example — Plantilla de variables de entorno para Lookaly
# =============================================================================
# INSTRUCCIONES:
#   1. Copia este archivo:  cp .env.example backend/.env
#   2. Rellena los valores reales (NUNCA uses los de este archivo en producción)
#   3. NUNCA hagas commit del .env real (está en .gitignore)
#
# En producción: usar AWS Secrets Manager, HashiCorp Vault, o variables de CI.
# =============================================================================

# ── Seguridad JWT (3.7) ───────────────────────────────────────────────
# Genera una clave segura (mínimo 256 bits):
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CAMBIA_ESTO_genera_con_secrets_token_hex_32

# ── Base de datos (3.7) ──────────────────────────────────────────────
# Formato: postgresql+asyncpg://usuario:contrasena@host:puerto/base_de_datos
# En producción: usar conexión SSL (postgresql+asyncpg://...?ssl=require)
DATABASE_URL=postgresql+asyncpg://lookaly:lookalypass@db:5432/lookaly_db

# ── App ───────────────────────────────────────────────────────────────────
# IMPORTANTE: cambiar a false en produccion para ocultar /docs y stack traces
DEBUG=true
FRONTEND_URL=http://localhost:5173

# ── Tokens JWT ────────────────────────────────────────────────────────
# Access token CORTO (3.4) — recomendado: 15-30 minutos
ACCESS_TOKEN_EXPIRE_MINUTES=30
# Refresh token LARGO (3.4) — recomendado: 7-14 días
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── Política de contraseñas (3.2) ─────────────────────────────────────────
# 0 = sin caducidad. En empresas: 90 ó 180 días.
PASSWORD_EXPIRE_DAYS=0
